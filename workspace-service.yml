server:
  port: 9020
  servlet.context-path: /workspace

spring:
  datasource:
    url: jdbc:postgresql://localhost:9010/codable-workspace-db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver