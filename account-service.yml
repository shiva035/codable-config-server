server:
  port: 9050
  servlet.context-path: /account

spring:
  datasource:
    url: jdbc:postgresql://localhost:9010/codable-account-db?options=-c%20timezone=Asia/Kolkata
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
